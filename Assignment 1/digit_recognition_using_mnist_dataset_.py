# -*- coding: utf-8 -*-
"""Digit Recognition using MNIST Dataset..ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1WU18qZWvPIHCkCZUEBSfnSlbJiuplbOy
"""

import tensorflow as tf
from tensorflow.keras import layers, models

# 1. Load and prepare the MNIST dataset
mnist = tf.keras.datasets.mnist
(x_train, y_train), (x_test, y_test) = mnist.load_data()

# Normalize pixel values to be between 0 and 1
x_train, x_test = x_train / 255.0, x_test / 255.0

# 2. Build the Neural Network
model = models.Sequential([
  layers.Flatten(input_shape=(28, 28)),     # Flatten 28x28 into 784
  layers.Dense(128, activation='relu'),     # Hidden layer with 128 neurons
  layers.Dropout(0.2),                      # Dropout to prevent overfitting
  layers.Dense(10, activation='softmax')    # Output layer (10 digits)
])

# 3. Compile the model
model.compile(optimizer='adam',
              loss='sparse_categorical_crossentropy',
              metrics=['accuracy'])

# 4. Train the model
print("Training model...")
model.fit(x_train, y_train, epochs=5)

# 5. Evaluate accuracy on the test set
print("\nEvaluating model...")
test_loss, test_acc = model.evaluate(x_test,  y_test, verbose=2)
print(f"Test Accuracy: {test_acc*100:.2f}%")

import numpy as np
from sklearn.metrics import confusion_matrix, ConfusionMatrixDisplay
import matplotlib.pyplot as plt

# Get predictions for the test set
predictions = model.predict(x_test)

# Convert predictions from probabilities to class labels
predicted_labels = np.argmax(predictions, axis=1)

# Calculate the confusion matrix
cm = confusion_matrix(y_test, predicted_labels)

# Display the confusion matrix
fig, ax = plt.subplots(figsize=(10, 10))
disp = ConfusionMatrixDisplay(confusion_matrix=cm, display_labels=np.arange(10))
disp.plot(cmap=plt.cm.Blues, ax=ax)
plt.title('Confusion Matrix')
plt.show()

"""# MNIST Digit Classifier with TensorFlow Keras

This notebook demonstrates how to build, train, and evaluate a simple Artificial Neural Network (ANN) using TensorFlow Keras to classify handwritten digits from the MNIST dataset.

## Project Structure

The notebook is structured into the following main sections:

1.  **Load and Prepare Data**: Downloads and preprocesses the MNIST dataset.
2.  **Build Neural Network**: Defines a sequential model with dense layers and dropout.
3.  **Compile Model**: Configures the model with an optimizer, loss function, and metrics.
4.  **Train Model**: Trains the neural network on the training data.
5.  **Evaluate Model**: Assesses the model's performance on the test set.
6.  **Confusion Matrix**: Visualizes the model's classification performance using a confusion matrix.
7.  **Classification Report**: Provides detailed per-class precision, recall, and F1-scores.

## Evaluation Results

After training for 5 epochs, the model achieved the following performance on the test set:

*   **Test Accuracy**: 97.92%

For a detailed breakdown of correct and incorrect classifications, refer to the **Confusion Matrix** section. Further insights into per-class performance (precision, recall, F1-score) are available in the **Classification Report** section.

## How to Use

To run this notebook and train your own MNIST digit classifier, follow these steps:

1.  **Open in Colab**: Open this notebook in Google Colab.
2.  **Run Cells**: Execute each code cell sequentially. The notebook will automatically download the MNIST dataset, train the model, and display the evaluation results, including the confusion matrix and classification report.
3.  **Experiment**: Feel free to modify the model architecture, hyperparameters (e.g., number of epochs, layer sizes), or optimizer settings to experiment with different configurations.

### Classification Report

Let's look at the precision, recall, and F1-score for each digit class.
"""

from sklearn.metrics import classification_report

# Generate the classification report
report = classification_report(y_test, predicted_labels, target_names=[str(i) for i in range(10)])

print("Classification Report:")
print(report)